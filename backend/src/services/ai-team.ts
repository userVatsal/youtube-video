export async function generateTitles(script: string): Promise<string[]> {
  return ['Suggested Title 1', 'Suggested Title 2'];
}

export async function generateTags(script: string): Promise<string[]> {
  return ['tag1', 'tag2'];
}
